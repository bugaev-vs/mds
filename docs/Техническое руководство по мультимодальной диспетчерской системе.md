**Версия:** 1.0
**Назначение:** руководство для пользователей (служб доставки, магазинов) и администраторов системы
**Область применения:** прототип мультимодальной диспетчерской системы для согласования маршрутов автономного городского транспорта

---
## Оглавление

1. Общие сведения о системе
2. Архитектура и компоненты
3. Роли и права доступа
4. Руководство пользователя (клиента)
5. Руководство администратора
6. Формат телеметрии
7. API-справочник
8. Обработка ошибок и коды ответов
9. Мониторинг и метрики
10. Развёртывание системы
11. Резервное копирование и восстановление
12. Безопасность
13. Диагностика и устранение неисправностей
14. Глоссарий

---

## 1. Общие сведения о системе

### 1.1. Назначение

Мультимодальная диспетчерская система (МДС) — программный комплекс для **согласования маршрутов автономного городского транспорта** четырёх типов:

- **Тротуарные роверы** — доставка по тротуарам и пешеходным зонам;
- **Беспилотные автомобили** — доставка по проезжей части;
- **Воздушные дроны** — доставка по воздушным коридорам;
- **Водные суда** — доставка по фарватерам и каналам.

Система **не управляет** транспортом. Она принимает заявки на маршрут, проверяет их на конфликты с препятствиями и другими маршрутами, согласовывает или отклоняет, ведёт карту занятости, принимает телеметрию и актуализирует маршруты в реальном времени.

### 1.2. Основные функции

| Функция | Описание |
|---|---|
| Регистрация клиентов | Приём и учёт служб доставки, магазинов |
| Приём заявок | Регистрация запросов на маршрут |
| Проверка маршрута | Анализ на препятствия и конфликты |
| Согласование | Решение: одобрено / одобрено с изменениями / отклонено |
| Карта занятости | Учёт всех согласованных маршрутов |
| Приём телеметрии | Получение данных о движении от клиентов |
| Детект отклонений | Сравнение фактического движения с планом |
| Актуализация маршрутов | Обновление временных окон в реальном времени |
| Визуализация | Карта города, временные окна, метрики |
| Журналирование | Запись всех событий системы |

### 1.3. Термины и определения

| Термин | Определение |
|---|---|
| **Клиент** | Служба доставки, магазин, зарегистрированный в системе |
| **Заявка** | Запрос на согласование маршрута |
| **Маршрут** | Последовательность точек (waypoints) |
| **Коридор** | Полоса пространства, занимаемая транспортом |
| **Временное окно** | Интервал, в течение которого маршрут активен |
| **Телеметрия** | Данные от клиента о движении транспорта |
| **Отклонение** | Расхождение между планом и фактом |
| **Препятствие** | Объект, блокирующий или замедляющий движение |
| **Карта занятости** | Структура данных с активными маршрутами |

---

## 2. Архитектура и компоненты

### 2.1. Компоненты системы

```
┌──────────────────────────────────────────────────────────┐
│                    КЛИЕНТЫ                               │
│  (службы доставки, магазины, аптеки)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ Модуль      │  │ Модуль      │  │ Модуль      │       │
│  │ телеметрии  │  │ заявок      │  │ визуализации│       │
│  └──────┬──────┘  └──────┬──────┘  └─────────────┘       │
└─────────┼────────────────┼───────────────────────────────┘
          │                │
          │ HTTPS/WSS      │ HTTPS
          ▼                ▼
┌──────────────────────────────────────────────────────────┐
│                    МДС (СЕРВЕР)                          │
│                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ API Gateway │  │ Валидатор   │  │ Реестр      │       │
│  │ (Flask)     │─►│ контрактов  │─►│ клиентов    │       │
│  └──────┬──────┘  └─────────────┘  └─────────────┘       │
│         │                                                │
│         ▼                                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ Диспетчер   │  │ Детектор    │  │ Актуализатор│       │
│  │ (ядро)      │◄►│ отклонений  │◄►│ маршрутов   │       │
│  └──────┬──────┘  └─────────────┘  └──────┬──────┘       │
│         │                                  │             │
│         ▼                                  ▼             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ CityMap     │  │ OccupancyMap│  │ Журнал      │       │
│  │ (карта)     │  │ (занятость) │  │ событий     │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                          │
│  ┌─────────────┐  ┌─────────────┐                        │
│  │ Визуализация│  │ Метрики     │                        │
│  │ (matplotlib)│  │ (аналитика) │                        │
│  └─────────────┘  └─────────────┘                        │
└──────────────────────────────────────────────────────────┘
```

### 2.2. Технологический стек

| Компонент      | Технология   |
| -------------- | ------------ |
| Язык           | Python 3.10+ |
| API            | Flask 3.x    |
| WebSocket      | websockets   |
| Валидация      | Pydantic 2.x |
| Геометрия      | Shapely 2.x  |
| Графы          | networkx     |
| Визуализация   | matplotlib   |
| Формат данных  | JSON         |
| Аутентификация | API-ключи    |

### 2.3. Требования к окружению

**Сервер:**
- CPU: 2 ядра и более;
- RAM: 4 ГБ и более;
- ОС: Linux (Ubuntu 22.04+), Windows 10+, macOS 12+;
- Python 3.10+;
- Свободные порты: 5000 (HTTP), 8765 (WebSocket).

**Клиент:**
- Любая ОС с Python 3.8+ или HTTP-клиентом;
- Стабильное интернет-соединение;
- API-ключ, выданный администратором.

---

## 3. Роли и права доступа

| Роль              | Права                                                                |
| ----------------- | -------------------------------------------------------------------- |
| **Клиент**        | Подача заявок, отправка телеметрии, просмотр статуса своих маршрутов |
| **Оператор**      | Просмотр всех заявок и маршрутов, ручное вмешательство               |
| **Администратор** | Управление клиентами, API-ключами, картой города, препятствиями      |
| **Аудитор**       | Просмотр журналов, метрик, отчётов (только чтение)                   |

---

## 4. Руководство пользователя (клиента)

### 4.1. Получение доступа

1. Обратитесь к администратору системы.
2. Предоставьте: название организации, ИНН, контактное лицо, список транспортных средств.
3. Получите **API-ключ** и **client_id**.
4. Сохраните их в защищённом месте.

### 4.2. Подача заявки на маршрут

**Эндпоинт:** `POST /api/v1/requests`

**Заголовки:**
```
Content-Type: application/json
X-API-Key: <ваш_api_ключ>
```

**Тело запроса:**
```json
{
  "request_id": "REQ-2025-001",
  "client_id": "pizza_shop_01",
  "transport_type": "sidewalk_rover",
  "waypoints": [
    {"x": 10, "y": 20, "z": 0},
    {"x": 30, "y": 35, "z": 0},
    {"x": 50, "y": 50, "z": 0}
  ],
  "departure_time": "2025-06-01T14:00:00Z",
  "cargo_weight": 2.5,
  "vehicle_params": {
    "width": 0.9,
    "max_slope": 15
  }
}
```

**Ответ (успех):**
```json
{
  "request_id": "REQ-2025-001",
  "status": "approved",
  "reason": "Маршрут свободен",
  "route_id": "R-001",
  "conflicts": []
}
```

**Ответ (с изменениями):**
```json
{
  "request_id": "REQ-2025-001",
  "status": "approved_with_changes",
  "reason": "Объезд препятствия stairs_1",
  "route_id": "R-002",
  "conflicts": ["stairs_1"]
}
```

**Ответ (отказ):**
```json
{
  "request_id": "REQ-2025-001",
  "status": "rejected",
  "reason": "Маршрут невозможен: коридор перегружен",
  "conflicts": ["OVERLOAD"]
}
```

### 4.3. Типы транспорта и параметры

| Тип              | Обязательные параметры | Описание               |
| ---------------- | ---------------------- | ---------------------- |
| `sidewalk_rover` | `width` (м)            | Тротуарный ровер       |
| `autonomous_car` | `height`, `mass`       | Беспилотный автомобиль |
| `aerial_drone`   | `max_alt`, `payload`   | Воздушный дрон         |
| `water_vessel`   | `draft`, `height`      | Водный катер           |

### 4.4. Отправка телеметрии

**Эндпоинт:** `POST /api/v1/telemetry`

**Частота:** каждые 1–5 секунд (рекомендуется 2 секунды).

**Тело запроса:**
```json
{
  "schema_version": "1.0",
  "client_id": "pizza_shop_01",
  "batch_id": "batch-2025-06-01-14-00-001",
  "sent_at": "2025-06-01T14:00:05Z",
  "vehicles": [
    {
      "vehicle_id": "rover_007",
      "route_id": "R-001",
      "transport_type": "sidewalk_rover",
      "timestamp": "2025-06-01T14:00:00Z",
      "position": {"x": 12.5, "y": 34.2, "z": 0.0},
      "speed": 1.4,
      "status": "moving",
      "progress": 0.35,
      "eta": "2025-06-01T14:12:00Z",
      "battery": 78.5,
      "alerts": []
    }
  ]
}
```

**Ответ:**
```json
{
  "status": "accepted",
  "batch_id": "batch-2025-06-01-14-00-001",
  "processed_vehicles": 1,
  "warnings": []
}
```

### 4.5. Статусы транспорта

| Статус    | Значение                    | Реакция диспетчера       |
| --------- | --------------------------- | ------------------------ |
| `moving`  | Движется по маршруту        | Обновление позиции       |
| `waiting` | Ожидает (светофор, очередь) | Продление окна           |
| `blocked` | Заблокирован                | Продление + reroute      |
| `arrived` | Прибыл                      | Освобождение коридора    |
| `lost`    | Потеряна связь              | Консервативное продление |

### 4.6. Алерты и события

| Код               | Severity | Действие клиента       |
| ----------------- | -------- | ---------------------- |
| `BLOCKED`         | high     | Сообщить о препятствии |
| `BATTERY_LOW`     | medium   | Запланировать возврат  |
| `ROUTE_DEVIATION` | medium   | Проверить навигацию    |
| `CARGO_TEMP_HIGH` | high     | Ускорить доставку      |
| `COLLISION_RISK`  | critical | Экстренная остановка   |

### 4.7. Типовые сценарии

**Сценарий 1: Обычная доставка**
1. Отправить заявку → получить `route_id`.
2. Начать движение, отправлять телеметрию каждые 2 сек.
3. При отклонении от плана — получить уведомление.
4. По прибытии — отправить `status: "arrived"`.

**Сценарий 2: Обнаружено препятствие**
1. В поле `alerts` отправить `["BLOCKED"]`.
2. В `status` указать `blocked`.
3. Дождаться решения диспетчера (продление окна, новый маршрут).

**Сценарий 3: Опоздание**
1. Диспетчер сам продлит окно по телеметрии.
2. Клиент получит уведомление в логе событий.

---

## 5. Руководство администратора

### 5.1. Установка системы

```bash
# 1. Клонировать репозиторий
git clone <repo_url>
cd dispatcher_prototype

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Запустить сервер
python main.py
```

### 5.2. Файл конфигурации

Создайте `config.yaml`:

```yaml
server:
  host: 0.0.0.0
  port: 5000
  ws_port: 8765
  debug: false

dispatcher:
  max_active_routes: 1000
  default_corridor_width: 2.0
  default_window_minutes: 20
  overload_threshold: 3

telemetry:
  max_batch_size: 1000
  max_age_seconds: 60
  deduplication_ttl: 3600

detector:
  threshold_time_sec: 120
  threshold_space_m: 5.0
  threshold_progress: 0.15

logging:
  level: INFO
  file: dispatcher.log
  max_size_mb: 100
  backup_count: 5

security:
  api_key_header: X-API-Key
  rate_limit_per_minute: 60
```

### 5.3. Управление клиентами

**Регистрация клиента:**

```bash
python admin_cli.py register-client \
  --client-id pizza_shop_01 \
  --name "Пиццерия №1" \
  --inn 7701234567 \
  --contact "ivanov@example.com"
```

**Выдача API-ключа:**

```bash
python admin_cli.py issue-key --client-id pizza_shop_01
# → API-ключ: key_abc123...
```

**Отзыв ключа:**

```bash
python admin_cli.py revoke-key --key-id key_abc123
```

**Список клиентов:**

```bash
python admin_cli.py list-clients
```

### 5.4. Управление картой города

**Добавить препятствие:**

```bash
python admin_cli.py add-obstacle \
  --id excavation_2 \
  --transport sidewalk_rover,autonomous_car \
  --geometry "LINESTRING(40 20, 40 40)" \
  --severity full_block \
  --active-from "08:00" \
  --active-to "20:00" \
  --description "Ремонт дороги"
```

**Удалить препятствие:**

```bash
python admin_cli.py remove-obstacle --id excavation_2
```

**Список препятствий:**

```bash
python admin_cli.py list-obstacles --transport aerial_drone
```

**Импорт карты из GeoJSON:**

```bash
python admin_cli.py import-map --file city_map.geojson
```

### 5.5. Мониторинг состояния

**Просмотр активных маршрутов:**

```bash
python admin_cli.py list-routes --status active
```

**Загруженность слоёв:**

```bash
python admin_cli.py occupancy-stats
# → {'sidewalk_rover': 4, 'autonomous_car': 2, 'aerial_drone': 3, 'water_vessel': 1}
```

**Журнал событий:**

```bash
python admin_cli.py events --last 100 --type deviation
```

**Метрики:**

```bash
python admin_cli.py metrics
# → telemetry_batches: 1250
#   telemetry_vehicles: 1250
#   deviations: 87
#   reroutes_requested: 12
```

### 5.6. Управление системой

**Остановка сервера:**

```bash
python admin_cli.py stop --graceful
# Мягкая остановка: завершает текущие заявки, освобождает ресурсы
```

**Перезапуск:**

```bash
python admin_cli.py restart
```

**Очистка старых данных:**

```bash
python admin_cli.py cleanup --older-than 30d
```

**Экспорт журнала:**

```bash
python admin_cli.py export-log --from "2025-06-01" --to "2025-06-02" --format csv
```

---

## 6. Формат телеметрии

### 6.1. Схема пакета

```json
{
  "schema_version": "1.0",
  "client_id": "string",
  "batch_id": "string",
  "sent_at": "ISO8601",
  "vehicles": [
    {
      "vehicle_id": "string",
      "route_id": "string",
      "transport_type": "enum",
      "timestamp": "ISO8601",
      "position": {"x": float, "y": float, "z": float},
      "speed": float,
      "heading": float,
      "status": "enum",
      "progress": float,
      "eta": "ISO8601",
      "battery": float,
      "cargo": {"weight": float, "type": "enum"},
      "alerts": ["string"]
    }
  ]
}
```

### 6.2. Ограничения

| Поле           | Ограничение               |
| -------------- | ------------------------- |
| `vehicles`     | 1–1000 элементов          |
| `speed`        | 0–100 м/с                 |
| `progress`     | 0.0–1.0                   |
| `battery`      | 0–100%                    |
| `cargo.weight` | 0–100 кг                  |
| `batch_id`     | уникальный в течение часа |

### 6.3. Валидация

Система проверяет:
1. Схему JSON (Pydantic).
2. Регистрацию клиента.
3. Принадлежность маршрута клиенту.
4. Свежесть пакета (< 60 сек).
5. Уникальность `batch_id`.
6. Монотонность `timestamp`.

При нарушении — пакет отклоняется с кодом 422 и списком ошибок.

---

## 7. API-справочник

### 7.1. Эндпоинты

| Метод  | Путь                         | Назначение                 |
| ------ | ---------------------------- | -------------------------- |
| POST   | `/api/v1/requests`           | Подача заявки              |
| GET    | `/api/v1/requests/{id}`      | Статус заявки              |
| DELETE | `/api/v1/requests/{id}`      | Отмена заявки              |
| POST   | `/api/v1/telemetry`          | Отправка телеметрии        |
| GET    | `/api/v1/routes/{id}`        | Информация о маршруте      |
| GET    | `/api/v1/routes/{id}/status` | Текущий статус             |
| GET    | `/api/v1/obstacles`          | Список препятствий         |
| GET    | `/api/v1/health`             | Проверка работоспособности |
| WS     | `/ws/telemetry`              | WebSocket-канал телеметрии |

### 7.2. Аутентификация

Все запросы требуют заголовок:
```
X-API-Key: <ваш_ключ>
```

При отсутствии или неверном ключе — `401 Unauthorized`.

### 7.3. Ограничения скорости

- По умолчанию: 60 запросов в минуту на клиента.
- При превышении — `429 Too Many Requests` с заголовком `Retry-After`.

### 7.4. Примеры запросов

**cURL:**
```bash
curl -X POST http://dispatcher.local/api/v1/requests \
  -H "Content-Type: application/json" \
  -H "X-API-Key: key_abc123" \
  -d @request.json
```

**Python:**
```python
import requests

response = requests.post(
    "http://dispatcher.local/api/v1/requests",
    json=request_data,
    headers={"X-API-Key": "key_abc123"}
)
print(response.json())
```

**JavaScript:**
```javascript
const response = await fetch("http://dispatcher.local/api/v1/requests", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-API-Key": "key_abc123"
  },
  body: JSON.stringify(requestData)
});
const result = await response.json();
```

---

## 8. Обработка ошибок и коды ответов

### 8.1. HTTP-коды

| Код | Значение               | Действие клиента        |
| --- | ---------------------- | ----------------------- |
| 200 | Успех                  | —                       |
| 201 | Создано                | —                       |
| 202 | Принято (телеметрия)   | —                       |
| 400 | Неверный запрос        | Исправить JSON          |
| 401 | Не авторизован         | Проверить API-ключ      |
| 403 | Доступ запрещён        | Проверить client_id     |
| 404 | Не найдено             | Проверить route_id      |
| 409 | Конфликт               | Повторить позже         |
| 422 | Ошибка валидации       | Исправить поля          |
| 429 | Слишком много запросов | Подождать               |
| 500 | Внутренняя ошибка      | Сообщить администратору |
| 503 | Сервис недоступен      | Повторить позже         |

### 8.2. Формат ошибки

```json
{
  "error": "validation_failed",
  "message": "Пакет телеметрии содержит ошибки",
  "details": [
    {"field": "vehicles[0].progress", "error": "must be <= 1.0"},
    {"field": "vehicles[0].timestamp", "error": "must be timezone-aware"}
  ],
  "request_id": "req-abc123"
}
```

### 8.3. Типовые ошибки

| Ошибка                    | Причина                         | Решение                    |
| ------------------------- | ------------------------------- | -------------------------- |
| `client_id mismatch`      | client_id не совпадает с ключом | Проверить поля             |
| `route not approved`      | Маршрут не согласован           | Подать заявку заново       |
| `batch already processed` | Дубликат batch_id               | Использовать уникальные ID |
| `stale packet`            | Пакет старше 60 сек             | Отправлять вовремя         |
| `overload`                | Коридор перегружен              | Сдвинуть вылет             |

---

## 9. Мониторинг и метрики

### 9.1. Ключевые метрики

| Метрика                        | Формула                     | Норма             |
| ------------------------------ | --------------------------- | ----------------- |
| **Задержка телеметрии**        | `receive_time - sent_at`    | < 5 сек           |
| **Частота отклонений**         | `deviations / packets`      | < 10%             |
| **Процент согласований**       | `approved / total_requests` | > 80%             |
| **Среднее время согласования** | `avg(check_duration)`       | < 100 мс          |
| **Загруженность слоя**         | `active_routes / capacity`  | < 70%             |
| **Количество reroute**         | `reroutes_requested`        | < 5% от маршрутов |

### 9.2. Дашборд (пример)

```
┌────────────────────────────────────────────────────┐
│  МДС — Мониторинг                                  │
├────────────────────────────────────────────────────┤
│  Активных маршрутов:            42 / 1000          │
│  Заявок за час:                 128                │
│  Согласовано:                   112 (87.5%)        │
│  Отклонено:                     16 (12.5%)         │
│  Отклонений от плана:           7 (5.5%)           │
│  Средняя задержка телеметрии:   1.8 сек            │
│                                                    │
│  Загруженность слоёв:                              │
│  ▓▓▓▓▓░░░░░  sidewalk_rover   12                   │
│  ▓▓▓░░░░░░░  autonomous_car    6                   │
│  ▓▓▓▓▓▓▓░░░  aerial_drone     18                   │
│  ▓░░░░░░░░░  water_vessel      3                   │
└────────────────────────────────────────────────────┘
```

### 9.3. Алерты администратору

| Условие                      | Уровень  | Действие               |
| ---------------------------- | -------- | ---------------------- |
| Задержка телеметрии > 10 сек | warning  | Проверить сеть клиента |
| Отклонений > 20%             | warning  | Проверить детектор     |
| Загруженность слоя > 90%     | critical | Расширить capacity     |
| Сервер не отвечает > 30 сек  | critical | Перезапуск             |
| Ошибок валидации > 100/час   | warning  | Связаться с клиентом   |

---

## 10. Развёртывание системы

### 10.1. Локальное развёртывание (разработка)

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```

### 10.2. Продакшн (Linux + systemd)

**Создать сервис** `/etc/systemd/system/dispatcher.service`:

```ini
[Unit]
Description=Multimodal Dispatcher Service
After=network.target

[Service]
Type=simple
User=dispatcher
WorkingDirectory=/opt/dispatcher
ExecStart=/opt/dispatcher/venv/bin/python main.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

**Активировать:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable dispatcher
sudo systemctl start dispatcher
sudo systemctl status dispatcher
```

### 10.3. Docker

**Dockerfile:**
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 5000 8765
CMD ["python", "main.py"]
```

**docker-compose.yml:**
```yaml
version: "3.9"
services:
  dispatcher:
    build: .
    ports:
      - "5000:5000"
      - "8765:8765"
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./logs:/app/logs
    restart: unless-stopped
```

**Запуск:**
```bash
docker-compose up -d
```

### 10.4. Проверка после развёртывания

```bash
curl http://localhost:5000/api/v1/health
# → {"status": "ok", "version": "1.0", "uptime_sec": 42}
```

---

## 11. Резервное копирование и восстановление

### 11.1. Что бэкапить

| Данные          | Частота       | Хранение  |
| --------------- | ------------- | --------- |
| Реестр клиентов | При изменении | 1 год     |
| Журнал событий  | Ежедневно     | 90 дней   |
| Карта города    | При изменении | 1 год     |
| Конфигурация    | При изменении | Постоянно |
| Метрики         | Ежедневно     | 1 год     |

### 11.2. Команды

**Создать бэкап:**
```bash
python admin_cli.py backup --output /backups/dispatcher-$(date +%F).tar.gz
```

**Восстановить:**
```bash
python admin_cli.py restore --input /backups/dispatcher-2025-06-01.tar.gz
```

**Автоматизация (cron):**
```cron
0 2 * * * /opt/dispatcher/venv/bin/python /opt/dispatcher/admin_cli.py backup --output /backups/dispatcher-$(date +\%F).tar.gz
```

---

## 12. Безопасность

### 12.1. Аутентификация и авторизация

- API-ключи генерируются с использованием `secrets.token_urlsafe(32)`.
- Ключи хранятся в виде хешей (bcrypt).
- Ротация ключей: раз в 90 дней.
- При компрометации — немедленный отзыв.

### 12.2. Транспортная безопасность

- HTTPS обязателен в продакшне.
- WSS для WebSocket.
- TLS-сертификат: Let's Encrypt.

### 12.3. Валидация входных данных

- Все данные проходят Pydantic-валидацию.
- Отклоняются пакеты с нереалистичными значениями.
- Ограничение размера тела запроса: 1 МБ.
- Санитизация всех строковых полей.

### 12.4. Ограничение скорости

- По умолчанию: 60 запросов в минуту на клиента.
- Для телеметрии: 30 пакетов в минуту.
- При превышении — блокировка на 60 секунд.

### 12.5. Аудит

Все действия записываются в журнал:
- Кто (client_id);
- Когда (timestamp);
- Что (тип операции);
- Результат (успех/ошибка);
- IP-адрес.

### 12.6. Рекомендации

- Не передавать API-ключи в открытом виде.
- Использовать переменные окружения.
- Регулярно обновлять зависимости.
- Мониторить подозрительную активность.

---

## 13. Диагностика и устранение неисправностей

### 13.1. Типовые проблемы

| Проблема                  | Возможная причина         | Решение              |
| ------------------------- | ------------------------- | -------------------- |
| `401 Unauthorized`        | Неверный API-ключ         | Проверить ключ       |
| `422 Validation error`    | Ошибка в JSON             | Проверить схему      |
| Телеметрия не принимается | Клиент не зарегистрирован | Зарегистрировать     |
| Маршрут отклонён          | Конфликт с препятствием   | Изменить маршрут     |
| Задержка телеметрии       | Сеть клиента              | Проверить соединение |
| Сервер не отвечает        | Падение процесса          | Перезапустить        |
| Высокая загрузка CPU      | Много активных маршрутов  | Масштабировать       |
| Переполнение диска        | Старые логи               | Очистить             |

### 13.2. Логи

**Расположение:** `/var/log/dispatcher/dispatcher.log`

**Уровни:**
- DEBUG — детальная отладка
- INFO — обычные события
- WARNING — потенциальные проблемы
- ERROR — ошибки
- CRITICAL — критические сбои

**Пример записи:**
```
2025-06-01 14:00:05 INFO  [dispatcher] Batch batch-001 accepted, 1 vehicle
2025-06-01 14:00:15 WARN  [detector] Deviation detected: veh_002, route R-002, progress
2025-06-01 14:00:30 ERROR [validator] Client unknown_client not registered
```

### 13.3. Диагностические команды

**Проверка здоровья:**
```bash
curl http://localhost:5000/api/v1/health
```

**Проверка БД:**
```bash
python admin_cli.py check-db
```

**Проверка индексов:**
```bash
python admin_cli.py check-indexes
```

**Тест валидации:**
```bash
python admin_cli.py test-validation --file test_batch.json
```

### 13.4. Контакты поддержки

- Email: support@dispatcher.local
- Телефон: +7 (XXX) XXX-XX-XX
- Часы работы: 9:00–18:00 МСК
- Экстренная линия: 24/7

---

## 14. Глоссарий

| Термин        | Определение                                               |
| ------------- | --------------------------------------------------------- |
| **API**       | Application Programming Interface — программный интерфейс |
| **Batch**     | Пакет телеметрии с одним или несколькими бортами          |
| **CCW**       | Counter-Clockwise — ориентационный тест                   |
| **ETA**       | Estimated Time of Arrival — прогноз прибытия              |
| **GeoJSON**   | Формат представления геоданных                            |
| **ISO 8601**  | Международный стандарт даты/времени                       |
| **JSON**      | JavaScript Object Notation — формат данных                |
| **MQTT**      | Message Queuing Telemetry Transport — протокол IoT        |
| **Occupancy** | Занятость — учёт активных маршрутов                       |
| **Pydantic**  | Библиотека валидации данных Python                        |
| **REST**      | Representational State Transfer — стиль API               |
| **R-tree**    | Пространственный индекс                                   |
| **Shapely**   | Библиотека геометрии Python                               |
| **TLS**       | Transport Layer Security — шифрование                     |
| **UTM**       | Unmanned Traffic Management — управление БПЛА             |
| **V2X**       | Vehicle-to-Everything — связь транспорта                  |
| **Waypoint**  | Точка маршрута                                            |
| **WebSocket** | Протокол двусторонней связи                               |
| **WSS**       | WebSocket Secure — защищённый WebSocket                   |

---

## Приложения

### Приложение А. Пример полного цикла

**Шаг 1. Клиент подаёт заявку:**
```bash
curl -X POST http://dispatcher.local/api/v1/requests \
  -H "X-API-Key: key_abc123" \
  -d '{"request_id":"REQ-001","client_id":"pizza_shop_01","transport_type":"sidewalk_rover","waypoints":[{"x":10,"y":20},{"x":30,"y":35}],"departure_time":"2025-06-01T14:00:00Z","vehicle_params":{"width":0.9}}'
```

**Шаг 2. Получает route_id:**
```json
{"request_id":"REQ-001","status":"approved","route_id":"R-001"}
```

**Шаг 3. Отправляет телеметрию:**
```bash
curl -X POST http://dispatcher.local/api/v1/telemetry \
  -H "X-API-Key: key_abc123" \
  -d '{"schema_version":"1.0","client_id":"pizza_shop_01","batch_id":"b-001","sent_at":"2025-06-01T14:00:05Z","vehicles":[{"vehicle_id":"veh_001","route_id":"R-001","transport_type":"sidewalk_rover","timestamp":"2025-06-01T14:00:00Z","position":{"x":12,"y":22},"status":"moving","progress":0.1}]}'
```

**Шаг 4. При прибытии:**
```bash
curl -X POST http://dispatcher.local/api/v1/telemetry \
  -H "X-API-Key: key_abc123" \
  -d '{"schema_version":"1.0","client_id":"pizza_shop_01","batch_id":"b-final","sent_at":"2025-06-01T14:15:00Z","vehicles":[{"vehicle_id":"veh_001","route_id":"R-001","transport_type":"sidewalk_rover","timestamp":"2025-06-01T14:15:00Z","position":{"x":30,"y":35},"status":"arrived","progress":1.0}]}'
```

### Приложение Б. Быстрый старт для администратора

```bash
# 1. Установка
git clone <repo> && cd dispatcher_prototype
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# 2. Конфигурация
cp config.example.yaml config.yaml
# Отредактировать config.yaml

# 3. Запуск
python main.py &

# 4. Регистрация первого клиента
python admin_cli.py register-client \
  --client-id demo_shop \
  --name "Демо-магазин"
python admin_cli.py issue-key --client-id demo_shop

# 5. Проверка
curl http://localhost:5000/api/v1/health
```

### Приложение В. Контрольный список перед продакшном

- [ ] Настроен HTTPS/WSS
- [ ] API-ключи выданы и защищены
- [ ] Настроено резервное копирование
- [ ] Настроен мониторинг
- [ ] Настроены алерты
- [ ] Проведены нагрузочные тесты
- [ ] Документация актуальна
- [ ] Контакты поддержки доступны
- [ ] План восстановления протестирован
- [ ] Логирование настроено

---

## Заключение

Данное руководство покрывает полный жизненный цикл работы с МДС: от установки и конфигурации до ежедневной эксплуатации и диагностики. Для пользователей описаны все необходимые операции по подаче заявок и отправке телеметрии. Для администраторов — управление клиентами, картой города, мониторинг и безопасность.

При возникновении вопросов, не описанных в руководстве, обращайтесь в службу поддержки.

**Версия документа:** 1.0
**Дата:** 2025-06-01
**Автор:** [ваше имя]
**Контакт:** [ваш email]