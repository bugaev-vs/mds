
## 1. Общая архитектура системы

```
┌────────────────────────────────────────────────────────────────┐
│                        КЛИЕНТЫ                                 │
│  Магазин / Служба доставки / Аптека / Ресторан                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ Роверы   │ │ Авто     │ │ Дроны    │ │ Катера   │           │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘           │
└───────┼────────────┼────────────┼────────────┼─────────────────┘
        │            │            │            │
        │   API заявок (JSON)     │            │
        ▼            ▼            ▼            ▼
┌────────────────────────────────────────────────────────────────┐
│                   ЯДРО ДИСПЕТЧЕРА                              │
│                                                                │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────────┐     │
│  │  Реестр     │  │  Валидатор   │  │   Маршрутизатор    │     │
│  │  клиентов   │→ │  заявки      │→ │  (по типу среды)   │     │
│  └─────────────┘  └──────────────┘  └──────────┬─────────┘     │
│                                                │               │
│         ┌──────────────────────────────────────┤               │
│         ▼              ▼               ▼       ▼               │
│  ┌───────────┐  ┌───────────┐  ┌──────────┐  ┌──────────┐      │
│  │ Sidewalk  │  │  Road     │  │   Air    │  │  Water   │      │
│  │ Checker   │  │  Checker  │  │ Checker  │  │ Checker  │      │
│  └─────┬─────┘  └─────┬─────┘  └────┬─────┘  └────┬─────┘      │
│        └──────────────┴─────────────┴─────────────┘            │
│                            │                                   │
│                            ▼                                   │
│              ┌──────────────────────────┐                      │
│              │  Модуль рекомендаций     │                      │
│              │  (объезд/сдвиг/отказ)    │                      │
│              └────────────┬─────────────┘                      │
│                           ▼                                    │
│              ┌──────────────────────────┐                      │
│              │  Единая карта занятости  │                      │
│              │  + Журнал согласований   │                      │
│              └──────────────────────────┘                      │
└────────────────────────────────────────────────────────────────┘
```

---

## 2. Диаграмма последовательности обработки заявки

```
Клиент        Валидатор     Checker      Recommender   OccupancyMap
  │              │             │              │              │
  │─submit──────►│             │              │              │
  │              │─validate───►│              │              │
  │              │             │─check zones─►│              │
  │              │             │              │              │
  │              │             │◄─conflicts───│              │
  │              │             │              │─query load──►│
  │              │             │              │◄─current─────│
  │              │             │              │              │
  │              │             │◄─recommend───│              │
  │              │◄─decision───│              │              │
  │◄─response────│             │              │              │
  │              │             │              │              │
  │              │             │              │─register────►│
  │              │             │              │  (если approved)
```

---

## 3. Базовые структуры данных

```python
# models.py
from dataclasses import dataclass, field
from enum import Enum
from typing import List, Tuple, Optional
from datetime import datetime, timedelta

class TransportType(Enum):
    SIDEWALK_ROVER = "sidewalk_rover"   # тротуарный ровер
    AUTONOMOUS_CAR = "autonomous_car"   # беспилотный автомобиль
    AERIAL_DRONE   = "aerial_drone"     # воздушный дрон
    WATER_VESSEL   = "water_vessel"     # водный катер

class Decision(Enum):
    APPROVED = "approved"
    APPROVED_WITH_CHANGES = "approved_with_changes"
    REJECTED = "rejected"
    PENDING = "pending"

@dataclass
class Waypoint:
    x: float
    y: float
    z: float = 0.0          # высота (для дронов) или глубина (для катеров)
    tag: Optional[str] = None   # "stairs", "bridge", "lock", "traffic_light"

@dataclass
class RouteRequest:
    request_id: str
    client_id: str
    transport_type: TransportType
    waypoints: List[Waypoint]
    departure_time: datetime
    cargo_weight: float = 1.0
    vehicle_params: dict = field(default_factory=dict)
    # для ровера: {"width": 0.9, "max_slope": 15}
    # для авто:  {"height": 1.8, "mass": 1500}
    # для дрона: {"max_alt": 120, "payload": 5}
    # для катера: {"draft": 1.5, "height": 4.0}
    status: Decision = Decision.PENDING
    reason: str = ""

@dataclass
class ApprovedRoute:
    route_id: str
    request_id: str
    transport_type: TransportType
    waypoints: List[Waypoint]
    time_window: Tuple[datetime, datetime]
    corridor_width: float = 3.0
    segments: List[str] = field(default_factory=list)  # ID сегментов
```

---

## 4. Карта города: препятствия и графы

```python
# city_map.py
from dataclasses import dataclass
from typing import List, Tuple, Optional
from shapely.geometry import LineString, Polygon, Point
from datetime import time

@dataclass
class Obstacle:
    obstacle_id: str
    transport_types: List[str]   # к каким средам относится
    geometry: object             # shapely Polygon или LineString
    active_from: time
    active_to: time
    severity: str = "full_block" # full_block / slowdown / restriction
    params: dict = None          # например {"max_height": 4.5}

class CityMap:
    def __init__(self):
        self.obstacles: List[Obstacle] = []
        # графы дорог для каждого типа транспорта
        self.graphs = {
            "sidewalk_rover": {},   # узел → список рёбер
            "autonomous_car": {},
            "water_vessel": {}
        }
    
    def add_obstacle(self, obs: Obstacle):
        self.obstacles.append(obs)
    
    def get_active_obstacles(self, transport_type: str, at_time: time) -> List[Obstacle]:
        result = []
        for obs in self.obstacles:
            if transport_type in obs.transport_types:
                if obs.active_from <= at_time <= obs.active_to:
                    result.append(obs)
        return result
```

### Пример наполнения карты

```python
from shapely.geometry import Polygon, LineString
from datetime import time

city = CityMap()

# --- Наземные препятствия (роверы) ---
city.add_obstacle(Obstacle(
    obstacle_id="stairs_1",
    transport_types=["sidewalk_rover"],
    geometry=Point(35, 42).buffer(0.5),
    active_from=time(0, 0), active_to=time(23, 59),
    severity="full_block",
    params={"reason": "Лестница на пути"}
))

city.add_obstacle(Obstacle(
    obstacle_id="narrow_1",
    transport_types=["sidewalk_rover"],
    geometry=LineString([(50,20), (60,20)]).buffer(0.2),
    active_from=time(0,0), active_to=time(23,59),
    severity="restriction",
    params={"min_width": 0.8}
))

city.add_obstacle(Obstacle(
    obstacle_id="crowd_lunch",
    transport_types=["sidewalk_rover"],
    geometry=Polygon([(70,30),(75,30),(75,35),(70,35)]),
    active_from=time(12,0), active_to=time(14,0),
    severity="slowdown",
    params={"density": "high", "delay_min": 10}
))

# --- Дорожные препятствия (автомобили) ---
city.add_obstacle(Obstacle(
    obstacle_id="excavation_1",
    transport_types=["autonomous_car"],
    geometry=LineString([(30,40),(30,60)]).buffer(2),
    active_from=time(8,0), active_to=time(20,0),
    severity="full_block",
    params={"reason": "Перекоп дороги"}
))

city.add_obstacle(Obstacle(
    obstacle_id="traffic_light_1",
    transport_types=["autonomous_car", "sidewalk_rover"],
    geometry=Point(50, 50).buffer(1),
    active_from=time(0,0), active_to=time(23,59),
    severity="slowdown",
    params={"cycle_green": 20, "cycle_red": 15, "avg_delay": 8}
))

# --- Воздушные препятствия ---
city.add_obstacle(Obstacle(
    obstacle_id="no_fly_hospital",
    transport_types=["aerial_drone"],
    geometry=Polygon([(40,40),(60,40),(60,60),(40,60)]),
    active_from=time(0,0), active_to=time(23,59),
    severity="full_block",
    params={"reason": "Больница — запретная зона"}
))

# --- Водные препятствия ---
city.add_obstacle(Obstacle(
    obstacle_id="drawbridge_1",
    transport_types=["water_vessel"],
    geometry=Point(60, 50).buffer(3),
    active_from=time(14,0), active_to=time(14,20),
    severity="restriction",
    params={"max_height": 3.0, "schedule": [("02:00","02:30"),("14:00","14:20")]}
))

city.add_obstacle(Obstacle(
    obstacle_id="shallow_1",
    transport_types=["water_vessel"],
    geometry=LineString([(20,60),(25,65)]).buffer(1),
    active_from=time(0,0), active_to=time(23,59),
    severity="restriction",
    params={"max_draft": 1.5}
))
```

---

## 5. Модуль проверки маршрута

Общая логика для всех типов, различия — в правилах.

```python
# checker.py
from shapely.geometry import LineString, Point
from datetime import datetime, timedelta

class RouteChecker:
    def __init__(self, city_map, occupancy, transport_type: str):
        self.city_map = city_map
        self.occupancy = occupancy
        self.transport_type = transport_type
    
    def check(self, request: RouteRequest) -> Tuple[Decision, str, List[Obstacle]]:
        conflicts = []
        recommendations = []
        
        route_line = LineString([(wp.x, wp.y) for wp in request.waypoints])
        t = request.departure_time.time()
        
        # 1. Проверка геометрических пересечений с препятствиями
        for obs in self.city_map.get_active_obstacles(self.transport_type, t):
            if self._intersects(route_line, obs, request):
                conflicts.append(obs)
                recommendations.append(self._make_recommendation(obs, request))
        
        # 2. Проверка занятости коридора
        load = self.occupancy.get_load(
            request.transport_type,
            request.waypoints,
            request.departure_time
        )
        if load >= self._max_capacity():
            conflicts.append("OVERLOAD")
            recommendations.append("Сдвинуть вылет на 15 минут")
        
        # 3. Решение
        if not conflicts:
            return Decision.APPROVED, "Маршрут свободен", []
        elif all(r for r in recommendations):
            return Decision.APPROVED_WITH_CHANGES, "; ".join(recommendations), conflicts
        else:
            return Decision.REJECTED, "Маршрут невозможен: " + "; ".join(recommendations), conflicts
    
    def _intersects(self, route_line, obstacle, request) -> bool:
        """Проверка пересечения с учётом параметров транспорта."""
        if not route_line.intersects(obstacle.geometry):
            return False
        
        # Дополнительные проверки по параметрам
        params = obstacle.params or {}
        vp = request.vehicle_params
        
        if "max_height" in params and vp.get("height", 0) > params["max_height"]:
            return True
        if "max_draft" in params and vp.get("draft", 0) > params["max_draft"]:
            return True
        if "min_width" in params and vp.get("width", 0) > params["min_width"]:
            return True
        
        return True
    
    def _make_recommendation(self, obs, request) -> str:
        if obs.severity == "full_block":
            return f"Объезд препятствия {obs.obstacle_id}"
        if obs.severity == "slowdown":
            return f"Замедление из-за {obs.obstacle_id}"
        if obs.severity == "restriction":
            return f"Ограничение: {obs.obstacle_id} (нужно изменить маршрут или время)"
        return "Проверить маршрут"
    
    def _max_capacity(self) -> int:
        return {"sidewalk_rover": 2, "autonomous_car": 3,
                "aerial_drone": 4, "water_vessel": 1}[self.transport_type]
```

---

## 6. Единая карта занятости

```python
# occupancy.py
from collections import defaultdict
from datetime import datetime, timedelta

class OccupancyMap:
    def __init__(self):
        # слой → ключ сегмента → список занятий
        self.layers = {
            "sidewalk_rover": defaultdict(list),
            "autonomous_car": defaultdict(list),
            "aerial_drone": defaultdict(list),
            "water_vessel": defaultdict(list),
        }
    
    def _segment_key(self, p1, p2) -> str:
        """Нормализованный ключ сегмента (порядок точек не важен)."""
        a = (round(p1.x, 1), round(p1.y, 1))
        b = (round(p2.x, 1), round(p2.y, 1))
        return f"{min(a,b)}__{max(a,b)}"
    
    def register(self, route: ApprovedRoute):
        layer = self.layers[route.transport_type.value]
        for i in range(len(route.waypoints) - 1):
            key = self._segment_key(route.waypoints[i], route.waypoints[i+1])
            layer[key].append({
                "route_id": route.route_id,
                "time": route.time_window,
                "width": route.corridor_width
            })
    
    def get_load(self, transport_type: str, waypoints, at_time: datetime) -> int:
        layer = self.layers[transport_type]
        max_load = 0
        window = (at_time, at_time + timedelta(minutes=20))
        for i in range(len(waypoints) - 1):
            key = self._segment_key(waypoints[i], waypoints[i+1])
            count = sum(
                1 for entry in layer.get(key, [])
                if self._overlap(entry["time"], window)
            )
            max_load = max(max_load, count)
        return max_load
    
    def _overlap(self, t1, t2) -> bool:
        return not (t1[1] < t2[0] or t2[1] < t1[0])
```

---

## 7. Главный диспетчер

```python
# dispatcher.py
class Dispatcher:
    def __init__(self, city_map: CityMap):
        self.city_map = city_map
        self.occupancy = OccupancyMap()
        self.requests: dict = {}
        self.approved: list = []
        self.registered_clients = set()
    
    def register_client(self, client_id: str):
        self.registered_clients.add(client_id)
    
    def submit_request(self, request: RouteRequest) -> dict:
        # 1. Регистрация
        if request.client_id not in self.registered_clients:
            return {"status": "rejected", "reason": "Клиент не зарегистрирован"}
        
        self.requests[request.request_id] = request
        
        # 2. Проверка
        checker = RouteChecker(
            self.city_map,
            self.occupancy,
            request.transport_type.value
        )
        decision, reason, conflicts = checker.check(request)
        
        # 3. Регистрация согласованного маршрута
        if decision in (Decision.APPROVED, Decision.APPROVED_WITH_CHANGES):
            route = ApprovedRoute(
                route_id=f"R-{len(self.approved)+1:03d}",
                request_id=request.request_id,
                transport_type=request.transport_type,
                waypoints=request.waypoints,
                time_window=(
                    request.departure_time,
                    request.departure_time + timedelta(minutes=30)
                )
            )
            self.occupancy.register(route)
            self.approved.append(route)
        
        return {
            "request_id": request.request_id,
            "status": decision.value,
            "reason": reason,
            "conflicts": [c.obstacle_id if hasattr(c, "obstacle_id") else c
                          for c in conflicts]
        }
```

---

## 8. Демонстрационный сценарий

```python
# demo.py
from datetime import datetime, timedelta

def run_demo():
    city = CityMap()
    # ... (наполнение карты из раздела 4) ...
    
    dispatcher = Dispatcher(city)
    dispatcher.register_client("pizza_shop")
    dispatcher.register_client("pharmacy")
    
    t0 = datetime(2025, 6, 1, 14, 0)
    
    scenarios = [
        # 1. Ровер: обычная доставка
        RouteRequest(
            request_id="REQ-001", client_id="pizza_shop",
            transport_type=TransportType.SIDEWALK_ROVER,
            waypoints=[Waypoint(10,10), Waypoint(20,15), Waypoint(30,25)],
            departure_time=t0,
            vehicle_params={"width": 0.9}
        ),
        # 2. Ровер: сталкивается с лестницей
        RouteRequest(
            request_id="REQ-002", client_id="pharmacy",
            transport_type=TransportType.SIDEWALK_ROVER,
            waypoints=[Waypoint(30,40), Waypoint(35,42), Waypoint(40,45)],
            departure_time=t0,
            vehicle_params={"width": 0.9}
        ),
        # 3. Авто: перекоп на пути
        RouteRequest(
            request_id="REQ-003", client_id="pizza_shop",
            transport_type=TransportType.AUTONOMOUS_CAR,
            waypoints=[Waypoint(25,35), Waypoint(30,50), Waypoint(35,65)],
            departure_time=t0,
            vehicle_params={"height": 1.8, "mass": 1500}
        ),
        # 4. Дрон: пересекает запретную зону
        RouteRequest(
            request_id="REQ-004", client_id="pharmacy",
            transport_type=TransportType.AERIAL_DRONE,
            waypoints=[Waypoint(35,45), Waypoint(50,50), Waypoint(65,55)],
            departure_time=t0,
            vehicle_params={"max_alt": 120, "payload": 2}
        ),
        # 5. Катер: мель на пути
        RouteRequest(
            request_id="REQ-005", client_id="pizza_shop",
            transport_type=TransportType.WATER_VESSEL,
            waypoints=[Waypoint(15,55), Waypoint(22,62), Waypoint(30,70)],
            departure_time=t0,
            vehicle_params={"draft": 2.0, "height": 4.0}
        ),
    ]
    
    for req in scenarios:
        result = dispatcher.submit_request(req)
        print(f"\n[{result['request_id']}] {result['status']}")
        print(f"   Причина: {result['reason']}")
        if result['conflicts']:
            print(f"   Конфликты: {result['conflicts']}")

if __name__ == "__main__":
    run_demo()
```

**Ожидаемый вывод:**

```
[REQ-001] approved
   Причина: Маршрут свободен

[REQ-002] approved_with_changes
   Причина: Объезд препятствия stairs_1
   Конфликты: ['stairs_1']

[REQ-003] approved_with_changes
   Причина: Объезд препятствия excavation_1
   Конфликты: ['excavation_1']

[REQ-004] approved_with_changes
   Причина: Объезд препятствия no_fly_hospital
   Конфликты: ['no_fly_hospital']

[REQ-005] approved_with_changes
   Причина: Ограничение: shallow_1 (нужно изменить маршрут или время)
   Конфликты: ['shallow_1']
```

---

## 9. Визуализация на matplotlib

```python
# visualize.py
import matplotlib.pyplot as plt
from matplotlib.patches import Polygon as MplPolygon, Circle
from shapely.geometry import Polygon as ShpPolygon

COLORS = {
    "sidewalk_rover": "#2ecc71",   # зелёный
    "autonomous_car": "#3498db",   # синий
    "aerial_drone":   "#e74c3c",   # красный
    "water_vessel":   "#9b59b6",   # фиолетовый
}

def draw_city(city_map, dispatcher, figsize=(14, 10)):
    fig, ax = plt.subplots(figsize=figsize)
    
    # 1. Препятствия
    for obs in city_map.obstacles:
        geom = obs.geometry
        color = {
            "full_block": "black",
            "slowdown": "orange",
            "restriction": "gray"
        }.get(obs.severity, "gray")
        
        if isinstance(geom, ShpPolygon):
            xs, ys = geom.exterior.xy
            ax.fill(xs, ys, alpha=0.3, color=color, label=obs.obstacle_id)
            ax.plot(xs, ys, color=color, linewidth=1.5)
        else:
            # Point.buffer → Polygon
            xs, ys = geom.exterior.xy
            ax.fill(xs, ys, alpha=0.3, color=color)
    
    # 2. Согласованные маршруты
    for route in dispatcher.approved:
        xs = [wp.x for wp in route.waypoints]
        ys = [wp.y for wp in route.waypoints]
        color = COLORS[route.transport_type.value]
        ax.plot(xs, ys, color=color, linewidth=2.5,
                marker='o', markersize=6,
                label=f"{route.route_id} ({route.transport_type.value})")
    
    # 3. Легенда и оформление
    handles, labels = ax.get_legend_handles_labels()
    seen = set()
    uniq = [(h, l) for h, l in zip(handles, labels) if not (l in seen or seen.add(l))]
    ax.legend(*zip(*uniq), loc="upper left", fontsize=9)
    
    ax.set_title("Мультимодальная диспетчерская: карта города",
                 fontsize=14, fontweight='bold')
    ax.set_xlabel("X (условные единицы)")
    ax.set_ylabel("Y (условные единицы)")
    ax.grid(True, linestyle=':', alpha=0.5)
    ax.set_aspect('equal')
    plt.tight_layout()
    plt.savefig("city_map.png", dpi=150)
    plt.show()
```

---

## 10. Диаграмма Ганта: загрузка коридоров

```python
# gantt.py
import matplotlib.pyplot as plt
import matplotlib.dates as mdates
from datetime import datetime, timedelta

def draw_gantt(dispatcher):
    fig, ax = plt.subplots(figsize=(14, 6))
    
    routes = dispatcher.approved
    for i, route in enumerate(routes):
        start = mdates.date2num(route.time_window[0])
        end = mdates.date2num(route.time_window[1])
        color = COLORS[route.transport_type.value]
        ax.barh(i, end - start, left=start, height=0.6,
                color=color, alpha=0.75, edgecolor='black')
        ax.text(start + 0.0005, i, f"{route.route_id}",
                va='center', fontsize=9, color='white', fontweight='bold')
    
    ax.set_yticks(range(len(routes)))
    ax.set_yticklabels([r.transport_type.value for r in routes])
    ax.xaxis.set_major_formatter(mdates.DateFormatter('%H:%M'))
    ax.set_xlabel("Время")
    ax.set_title("График согласованных маршрутов (Gantt)",
                 fontweight='bold')
    ax.grid(True, axis='x', linestyle=':', alpha=0.6)
    plt.tight_layout()
    plt.savefig("gantt.png", dpi=150)
    plt.show()
```

---

## 11. Аналитический график: загрузка по средам

```python
# analytics.py
import matplotlib.pyplot as plt
from collections import Counter

def draw_load_analytics(dispatcher):
    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 5))
    
    # 1. Круговая: распределение по типам транспорта
    types = [r.transport_type.value for r in dispatcher.approved]
    counts = Counter(types)
    ax1.pie(counts.values(), labels=counts.keys(),
            autopct='%1.0f%%',
            colors=[COLORS[k] for k in counts.keys()],
            startangle=90)
    ax1.set_title("Согласованные маршруты по типам транспорта",
                  fontweight='bold')
    
    # 2. Столбчатая: количество заявок по статусам
    statuses = Counter(r.status.value for r in dispatcher.requests.values())
    ax2.bar(statuses.keys(), statuses.values(),
            color=['#2ecc71', '#f39c12', '#e74c3c', '#95a5a6'][:len(statuses)])
    ax2.set_title("Распределение заявок по статусам", fontweight='bold')
    ax2.set_ylabel("Количество")
    for i, v in enumerate(statuses.values()):
        ax2.text(i, v + 0.05, str(v), ha='center', fontweight='bold')
    
    plt.tight_layout()
    plt.savefig("analytics.png", dpi=150)
    plt.show()
```

---

## 12. Диаграмма классов (UML-подобная)

```
┌─────────────────────┐        ┌─────────────────────┐
│     CityMap         │        │     Dispatcher      │
├─────────────────────┤        ├─────────────────────┤
│ - obstacles: List   │◄───────│ - city_map: CityMap │
│ - graphs: dict      │        │ - occupancy: Map    │
├─────────────────────┤        │ - requests: dict    │
│ + add_obstacle()    │        │ - approved: list    │
│ + get_active()      │        ├─────────────────────┤
└─────────────────────┘        │ + register_client() │
                               │ + submit_request()  │
                               └──────────┬──────────┘
                                          │ uses
              ┌───────────────────────────┼──────────────────┐
              ▼                           ▼                  ▼
    ┌──────────────────┐        ┌──────────────────┐ ┌──────────────┐
    │  RouteChecker    │        │  OccupancyMap    │ │  Approved    │
    ├──────────────────┤        ├──────────────────┤ │  Route       │
    │ - transport_type │        │ - layers: dict   │ ├──────────────┤
    ├──────────────────┤        ├──────────────────┤ │ - waypoints  │
    │ + check()        │        │ + register()     │ │ - window     │
    │ + _intersects()  │        │ + get_load()     │ │ - segments   │
    │ + _recommend()   │        └──────────────────┘ └──────────────┘
    └──────────────────┘
```

---

## 13. Схема потоков данных

```
   ┌──────────────┐
   │   Клиент     │
   │ (магазин)    │
   └──────┬───────┘
          │ RouteRequest (JSON)
          ▼
   ┌──────────────────┐
   │  API Диспетчера  │
   └──────┬───────────┘
          │
          ▼
   ┌──────────────────┐    активные    ┌──────────────────┐
   │ RouteChecker     │◄───────────────│   CityMap        │
   │ (для среды)      │   препятствия  │ (препятствия)    │
   └──────┬───────────┘                └──────────────────┘
          │
          │ запрос загрузки
          ▼
   ┌──────────────────┐
   │ OccupancyMap     │
   │ (занятость)      │
   └──────┬───────────┘
          │ текущая загрузка
          ▼
   ┌──────────────────┐
   │ Decision +       │
   │ Recommendation   │
   └──────┬───────────┘
          │
          ▼
   ┌──────────────────┐
   │ Ответ клиенту    │
   │ + журнал         │
   └──────────────────┘
```

---

## 14. Что показать на защите

1. **Слайд 1.** Постановка задачи: единая диспетчерская для 4 типов транспорта.
2. **Слайд 2.** Архитектура (раздел 1) и UML (раздел 12).
3. **Слайд 3.** Структуры данных (раздел 3) и пример препятствий (раздел 4).
4. **Слайд 4.** Демо-сценарий (раздел 8) — вывод консоли.
5. **Слайд 5.** Визуализация карты (раздел 9) — PNG.
6. **Слайд 6.** График Ганта (раздел 10) и аналитика (раздел 11).
7. **Слайд 7.** Выводы: масштабируемость, единый API, реальные прототипы.

---

## 15. Как это развивать дальше

- **Динамика в реальном времени** — pygame-анимация, где дроны двигаются по согласованным маршрутам.
- **Приоритеты** — срочная доставка медикаментов получает приоритет.
- **Пересмотр маршрутов** — если появляется новое препятствие, система ищет альтернативы для уже согласованных заявок.
- **Машинное обучение** — прогноз загруженности на основе исторических данных.
- **Веб-интерфейс** — Flask + простой HTML для подачи заявок магазинами.

---

## Итог

Ты получаешь **рабочий прототип мультимодальной диспетчерской**:
- 4 типа транспорта (ровер, авто, дрон, катер);
- единая карта препятствий;
- модуль проверки с рекомендациями;
- единая карта занятости;
- визуализация: карта, Ганта, аналитика.

Код структурирован по модулям, каждый можно объяснить отдельно.